<div class="conversation">
    {{#if name}}
    <div class="conversation__header">
        <div class="conversation__avatar">
            {{#if avatarURL}}
            <img class="conversation__avatar_image" src="{{avatarURL}}" alt="avatar for {{name}}"/>
            {{else}}
            <img class="conversation__avatar_image" src="{{defualtAvatarURL}}" alt="no image avatar"/>
            {{/if}}
        </div>

        <div class="conversation__name">
            {{name}}
        </div>

        {{{buttonOptions}}}
    </div>

    <div class="conversation__content">
        {{{chatFeed}}}
    </div>

    <div class="conversation__footer">
        {{{sendMessageForm}}}
    </div>
    {{else}}
    <span>Выберите чат чтобы отправить сообщение</span>
    {{/if}}
</div>
